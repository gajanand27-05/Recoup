# ---------------------------------------------------------------------------
# LOCAL-ONLY WORKING DOCUMENTS — never pushed.
# See CLAUDE.md section 2. If a file should move between local and shipped,
# change it here, not by remembering.
# ---------------------------------------------------------------------------
CLAUDE.md
PLAN.md
DECISION.md
LOGS.md
SUBMISSION.md
VIDEO.md
docs/superpowers/

# ---------------------------------------------------------------------------
# Secrets
# ---------------------------------------------------------------------------
.env
.env.local
*.pem
*.key

# ---------------------------------------------------------------------------
# Run outputs
# ---------------------------------------------------------------------------
runs/
*.db
*.sqlite3

# ...EXCEPT run provenance. A figure has to name the commit that produced it, and
# provenance that does not ship is not provenance -- a judge would see the number
# and never the pinning. These files are small, contain no run data, and are the
# only thing standing between "the lift is X" and "the lift is X, made by THIS
# code, reproducible under HEAD or not". See CLAUDE.md section 4 and INC-011.
!runs/
runs/*
!runs/*.provenance.json

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# ---------------------------------------------------------------------------
# Editors / OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
